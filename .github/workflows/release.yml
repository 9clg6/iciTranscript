name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-macos:
    name: Build & Release macOS DMG
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.38.4"
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get
        working-directory: apps/app_macos

      - name: Build macOS release
        run: flutter build macos --release
        working-directory: apps/app_macos

      - name: Sign app (ad-hoc)
        run: |
          codesign --sign - --deep --force \
            "apps/app_macos/build/macos/Build/Products/Release/iciTranscript.app"

      - name: Install create-dmg
        run: brew install create-dmg

      - name: Extract version from tag
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Create DMG
        run: |
          create-dmg \
            --volname "IciTranscript" \
            --window-pos 200 120 \
            --window-size 660 400 \
            --icon-size 80 \
            --icon "iciTranscript.app" 180 170 \
            --hide-extension "iciTranscript.app" \
            --app-drop-link 480 170 \
            --no-internet-enable \
            "IciTranscript-${{ github.ref_name }}-macos.dmg" \
            "apps/app_macos/build/macos/Build/Products/Release/iciTranscript.app"

      - name: Create ZIP
        run: |
          ditto -c -k --sequesterRsrc --keepParent \
            "apps/app_macos/build/macos/Build/Products/Release/iciTranscript.app" \
            "IciTranscript-${{ github.ref_name }}-macos.zip"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            IciTranscript-${{ github.ref_name }}-macos.dmg
            IciTranscript-${{ github.ref_name }}-macos.zip
          generate_release_notes: true
          body: |
            ## Installation

            1. Télécharger `IciTranscript-${{ github.ref_name }}-macos.dmg`
            2. Ouvrir le DMG et glisser IciTranscript dans Applications
            3. **Clic droit > Ouvrir** (première fois, Gatekeeper avertit car signature ad-hoc)
            4. L'app vous guide pour installer `uv` si nécessaire

            ## Prérequis

            - macOS 13+ (Apple Silicon recommandé pour MLX)
            - `uv` — installé automatiquement par l'app au premier lancement
